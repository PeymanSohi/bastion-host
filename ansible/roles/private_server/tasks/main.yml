- name: Update apt cache
  apt:
    update_cache: yes

- name: Install required packages
  apt:
    name: htop
    state: present

- name: Copy SSH config (optional)
  copy:
    src: sshd_config
    dest: /etc/ssh/sshd_config
    backup: yes
  notify: Restart SSH
